/*
				iTween.ValueTo(thisGameObject, iTween.Hash(
					"onUpdate", "fadeKaedeBig"
					,"from", Kaede_big.transform.position.x + 50
					, "to", Kaede_big.transform.position.x
					, "delay", 0
					, "time", .75f
				));
			*/

      /*
		Debug.Log ("onFade..." + alpha);
		Image img = Kaede_big.gameObject.GetComponent<Image> ();
		Color newColor = img.color;
		newColor.a = alpha;
		img.color = newColor;
		*/